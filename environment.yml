name: ftptomongo  # Replace 'myenv' with your desired environment name
channels:
  - defaults
dependencies:
  - python=3.10  # Specify the Python version you want
  - pip
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - jupyter
  - flask
  - usutil
  - pyftpdlib